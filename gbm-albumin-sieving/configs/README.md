# Configurations

Version-controlled run configurations belong here. Large data paths and machine-local
absolute paths should remain outside Git.

The `reproducibility/` directory contains the random seeds and small deterministic
inputs required to reproduce the principal simulation experiments.
