# Figure 3: SD spacing and sieving

Run:

`python workflows/plot_fig3G.py`

The frozen CSV/JSON inputs contain the fitted SD-width density, observed SD
widths, orientation-bounded accessible-area estimates, and integrated midpoint
sieving summary. The figure output is `fig3G_orientation_bounds.png` with
matching SVG and PDF exports.
