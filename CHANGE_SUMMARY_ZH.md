# 本地更新说明

本目录是准备替换 GitHub 仓库内容的候选版本。原始科研工作目录没有被覆盖，
所有面向公开仓库的修改均集中在本目录。

## 已完成的更新

### GBM

- 加入稿件实际使用的 DD2006 守恒输运求解器及单网络入口。
- 将排斥半径与水动力半径分开：4.25 nm 只控制几何可达性和分配系数，
  `K_D`、`K_C` 固定使用 3.55 nm 水合半径。
- 静电半径批处理流程改为显式传入两种半径。
- 保留旧求解器用于历史追溯，并在文档中标明它不是最终静电结果入口。
- 加入 100 网络演示的说明、清单、总体对照和验证摘要；大型数据包放在
  GitHub Release，不进入普通 Git 提交。

### SD

- 加入从 SD 宽度、柔性六边形和三维白蛋白取向重算上下界的完整流程。
- 稿件设置固定为 101 个宽度点、每点 5,000 个随机取向、2 度确定性扫描和
  固定随机种子。
- 明确并实现“下限和上限分别积分、分别换算筛过系数、最后取算术平均”。
- 删除历史汇总表覆盖新计算结果的路径；宽度改变后若未同步重算取向表，
  程序会直接报错。

### 整体 GFB

- 新增 `gfb-integration/`，用共同的 `Q_water` 和 `Q_albumin` 串联
  plasma bulk、GBM、GBM-to-SD bulk 和 SD。
- 四个条件均输出层间浓度、传递比、共同流量和方程残差。
- 图中四舍五入系数的乘积只作为对照列，守恒模型结果由方程联立求得。

### Nature 材料

- 新增稿件结果到代码的对应表、Nature 清单状态表、验证报告和上传指南。
- 更新 `gbm-demo-100.zip`，使独立求解器即使不显式填写水动力半径，也默认
  使用 3.55 nm。
- 公开演示不写网络筛选规则；详细筛选记录保留在作者内部审阅材料中。

## 相对当前本地仓库副本的文件变化

- GBM：新增 8 个文件，修改 5 个文件，没有删除原文件。主要新增最终
  DD2006 求解器、单网络入口、精确依赖和演示说明/清单/验证摘要。
- SD：新增 9 个文件，修改 3 个文件，没有删除原文件。主要新增完整取向
  几何链、101 点冻结结果元数据、精确依赖和重算入口。
- 仓库根目录：新增 `gfb-integration/`、稿件结果映射、Nature 状态表、
  验证报告、中文变更说明和上传指南。

因此上传时应复制整个候选结构，而不是只替换一个求解脚本。候选版本保留
旧 GBM 求解器作为 historical 文件，便于追溯已有结果。

## 当前不需要处理的两个异常网络

`synthetic_AS_run2490_plane` 和 `synthetic_AS_run1466_plane` 都不在最终
100 网络演示清单中。`run1466` 已由同一厚度/水流量分层中的 `run1533`
替代，且后者通过两种排斥半径。因此当前演示不需要再次生成或替换网络。

## 发布后仍需作者决定或补充

1. 选择软件许可证并加入仓库根目录 `LICENSE`。
2. 全新环境安装已经通过；仍建议由不熟悉软件的同事独立重复一次。
3. 确认最终稿件图号，并补齐结果映射表中标为 pending 的最终拟合输入、
   图源数据和剩余绘图入口。
4. Release `gbm-example-v1.0.0` 已发布；本清理提交已修复 GBM 示例 README
   中的下载地址，并删除旧缓存和生成输出，尚需合并到公开 `main`。
